#include <stdio.h>

int main()
{
	int a, b;
	printf("숫자 두개를 입력하세요.");
	scanf_s("%d%d", &a, &b);

	if (a > b)
	{
		printf("%d는 %d보다 큰 수 입니다.", a, b);
	}
	else if (a < b)
	{
		printf("%d는 %d보다 큰 수 입니다.", b, a);
	}
	else
	{
		printf("두 수는 같은 수 입니다.");
	}

}